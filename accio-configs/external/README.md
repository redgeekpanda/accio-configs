# Accio external configs

Files out of user's home directory.

## Link external files

Example: create hard link for repository version of blurlock script to external version.

```shell
# Create hard link
sudo ln --force blurlock /usr/bin/blurlock
```
